# Minimal Example

This example keeps the architecture intentionally small:

- `communicator`: the only user-facing agent
- `planner`: breaks work into executable tasks
- `coder`: implements the requested changes

The config keeps every relevant field explicit so it can be used as a reference when tuning a generated project.
